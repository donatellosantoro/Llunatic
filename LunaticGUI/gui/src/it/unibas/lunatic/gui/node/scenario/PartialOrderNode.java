package it.unibas.lunatic.gui.node.scenario;

import org.openide.nodes.FilterNode;
import org.openide.nodes.Node;

public class PartialOrderNode extends FilterNode {
    
    public PartialOrderNode(Node nodeDelegate) {
        super(nodeDelegate);
    }
}
